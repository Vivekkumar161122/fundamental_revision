# Python has also a build-in module called math, which extends the list of mathematical functions.abs
import math

# The math.sqrt() method 
import math
x = math.sqrt(64)
print(x)

import math
# The math.ceil() method rounds a number upwards to it's nearest integer and the math.floor() method rounds a number downwards to it's nearest integer and returns the result.
x = math.ceil(1.4)
y = math.floor(1.4)

print(x)
print(y)

# The math.pi constant, returns the value of PI (3.14.....)
import math
x = math.pi
print(x)
